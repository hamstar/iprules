#!/bin/bash

source /etc/iptrules.conf;

for RULE in $E_PATH/*;
  do ipt -d `basename $RULE`;
done;

if [ "$?" != "0" ]; then
  echo "An error occured while flushing the rules";
  exit 1;
fi;

echo "Successfully disabled all rules";
