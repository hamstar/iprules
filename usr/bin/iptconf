#!/bin/bash

source /etc/iptrules.conf

echo "# Generated by iptrules "`date +"%Y-%m-%d %H:%M:%S"`;
echo "*filter";
cat $POLICY_FILE;

if [ "$(ls $E_PATH|wc -l)" != "0" ]; then
  cat $E_PATH/*;
else
  echo "# no rules enabled";
fi;

echo "COMMIT";
