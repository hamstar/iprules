#!/bin/bash

source /etc/iptrules.conf

if [ "$1" = "-a" ]; then
  echo "Currently available rules";
  ls $A_PATH;
else
  echo "Currently enabled rules";
  if [ "$(ls $E_PATH|wc -l)" = "0" ]; then
    echo "(none)";
  else
    ls $E_PATH;
  fi;
fi;
